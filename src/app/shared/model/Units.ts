export class Units{
  constructor(unitId: number, unitName: String, number: number, number2: number, number3: number) {

  }

  unitId!: number;
    unitName!: string;
    inPrice!: number;
    outPrice!: number;
    parentId!:number
    amount!: number
}
